mainClassName = 'io.gitlab.arturbosch.quide.platform.PlatformKt'

apply plugin: 'groovy'

configurations {
	compile.extendsFrom kotlin
	testCompile.extendsFrom spock
}

dependencies {
	compile "io.gitlab.arturbosch:kutils:$kutilsVersion"
	compile project(":quide-specification")
}
